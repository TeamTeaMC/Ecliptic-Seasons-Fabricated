{
  "pack": {
    "pack_format": 15,
    "supported_formats": { "min_inclusive": 15, "max_inclusive": 999 },
    "min_format": [ 15,0 ],
    "max_format": [ 999,0 ],
    "description": "§bThe old snow model is slightly offset from the original one, preventing it from being affected by special model variants such as CTM. However, at long distances, it may flicker due to floating-point precision issues. It is recommended to enable CullTopFaceWithSnow when using it."
  }
}
